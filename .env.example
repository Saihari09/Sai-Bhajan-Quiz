# Bhajan Bodh — leaderboard backend (Supabase)
# 1. Create a free project at https://supabase.com
# 2. In the SQL editor, run supabase/schema.sql
# 3. Authentication → Sign In / Up → enable "Anonymous sign-ins"
# 4. Copy Project URL + anon public key here (and into GitHub Actions
#    secrets VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY for deploys)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
